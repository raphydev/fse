{% extends 'fse/fse_layout.html.twig' %}

{% block title %} {{ 'Classification des spornsors & partenairess' }} {% endblock %}
{% block fse_title %} {{ '| Classification des spornsors & partenairess'|upper }}{% endblock %}

{% block fse_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér une nouvelle classification'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('classification_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('classification/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}