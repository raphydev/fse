{% extends 'base.html.twig' %}

{% block title %}Domain{% endblock %}

{% block body %}
    <h1>Domain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domain.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ domain.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('domain_index') }}">back to list</a>

    <a href="{{ path('domain_edit', {'id': domain.id}) }}">edit</a>

    {{ include('domain/_delete_form.html.twig') }}
{% endblock %}