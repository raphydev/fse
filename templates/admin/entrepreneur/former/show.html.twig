{% extends 'base.html.twig' %}

{% block title %}Former{% endblock %}

{% block body %}
    <h1>Former</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ former.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ former.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ former.slug }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ former.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('former_index') }}">back to list</a>

    <a href="{{ path('former_edit', {'id': former.id}) }}">edit</a>

    {{ include('former/_delete_form.html.twig') }}
{% endblock %}