{% extends 'base.html.twig' %}

{% block title %}Legal{% endblock %}

{% block body %}
    <h1>Legal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ legal.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ legal.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('legal_index') }}">back to list</a>

    <a href="{{ path('legal_edit', {'id': legal.id}) }}">edit</a>

    {{ include('legal/_delete_form.html.twig') }}
{% endblock %}