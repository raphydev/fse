{% block title %} {{ 'Programme du forum' }} {% endblock %}
{% block forum_title %} {{ '| Programme du forum'|upper }}{% endblock %}
{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier programme'|upper }}</h4>
                {{ include('admin/forum/program/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('admin/forum/program/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}