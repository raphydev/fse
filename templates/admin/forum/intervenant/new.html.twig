{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Intervenant' }} {% endblock %}
{% block forum_title %} {{ '| Intervenants'|upper }}{% endblock %}

{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér un nouveau intervenant'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('intervenant_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('intervenant/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}