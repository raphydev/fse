<form method="post" action="{{ path('organizer_delete', {'id': organizer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organizer.id) }}">
    <button class="btn btn-float btn-danger"><i class="ti-trash"></i></button>
</form>