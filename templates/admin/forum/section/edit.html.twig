{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Section du forum' }} {% endblock %}
{% block forum_title %} {{ '| Section de forum'|upper }}{% endblock %}
{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier la section'|upper }}</h4>
                {{ include('admin/forum/section/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('admin/forum/section/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}

