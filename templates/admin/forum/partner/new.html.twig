{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Partenaire' }} {% endblock %}
{% block forum_title %} {{ '| Partenaire'|upper }}{% endblock %}

{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér un nouveau  partenaire'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('partner_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('admin/forum/partner/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}