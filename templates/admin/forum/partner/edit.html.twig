{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Partenaire' }} {% endblock %}
{% block forum_title %} {{ '| Partenaire'|upper }}{% endblock %}

{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier partenaire'|upper }}</h4>
                {{ include('admin/forum/partner/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('admin/forum/partner/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}