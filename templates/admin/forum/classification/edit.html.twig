{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Classification des spornsors & partenaires' }} {% endblock %}
{% block forum_title %} {{ '| Classification des spornsors & partenaires'|upper }}{% endblock %}

{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier classification'|upper }}</h4>
                {{ include('classification/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('classification/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                    })
                }}
            </div>
        </div>
    </div>
{% endblock %}

