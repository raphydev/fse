{% block title %} {{ 'Date du forum' }} {% endblock %}
{% block forum_title %} {{ '| Date du forum'|upper }}{% endblock %}
{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier date'|upper }}</h4>
                {{ include('admin/forum/schecule/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('admin/forum/schecule/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}