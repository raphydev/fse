{% extends 'admin/forum/forum_layout.html.twig' %}

{% block title %} {{ 'Article' }} {% endblock %}
{% block forum_title %} {{ '| Article'|upper }}{% endblock %}

{% block forum_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier article'|upper }}</h4>
                {{ include('post/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('post/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}