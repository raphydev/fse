{% extends 'admin/admin.html.twig' %}
{% block title %} {{ 'Edit Users' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier les infos du user'|upper }}</h4>
                {{ include('admin/users/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('admin/users/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}