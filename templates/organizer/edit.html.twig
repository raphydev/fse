{% extends 'fse/fse_layout.html.twig' %}

{% block title %} {{ 'Organisateurs' }} {% endblock %}
{% block fse_title %} {{ '| Organisateurs'|upper }}{% endblock %}

{% block fse_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier Organisateur'|upper }}</h4>
                {{ include('organizer/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('organizer/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}