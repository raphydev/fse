{% extends 'fse/fse_layout.html.twig' %}

{% block title %} {{ 'Organisateurs' }} {% endblock %}
{% block fse_title %} {{ '| Organisateurs'|upper }}{% endblock %}

{% block fse_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér un nouveau organisateur'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('organizer_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('organizer/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}