{% extends 'fse/fse_layout.html.twig' %}

{% block title %} {{ 'Type de partenaires' }} {% endblock %}
{% block fse_title %} {{ '| Type de partenaire'|upper }}{% endblock %}

{% block fse_content %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier Type de partenaire'|upper }}</h4>
                {{ include('partner_type/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('partner_type/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                    })
                }}
            </div>
        </div>
    </div>
{% endblock %}

