<form method="post" action="{{ path('partner_type_delete', {'id': partner_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner_type.id) }}">
    <button class="btn btn-float btn-danger"><i class="ti-trash"></i></button>
</form>